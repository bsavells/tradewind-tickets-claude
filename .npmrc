# vite-plugin-pwa@1.2.0 declares peer-dep on vite <=7 but works fine with
# vite 8 in practice. Until the plugin bumps its peer range, opt npm into
# the legacy resolver so local installs and Vercel CI behave the same.
legacy-peer-deps=true
